import { defineConfig } from 'vite'

// Ensure the site is served from the repository subpath on GitHub Pages.
// Update this value if you rename the repository.
export default defineConfig({
  base: '/portfolio-experiment/'
})
